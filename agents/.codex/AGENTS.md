# Coding

Work in progress rule:
* When working long plans with multi step/phases/tasks/sections/parts/etc don't mention its numbers/letters/counts in code comments, commit message, or documentation. Commiting each step is allowed, as long as commit message conveys what was done, and not the planning mechanics
* Never write unit tests unless directly prompted by the user. But do update existing ones when updating code it tests. After the code is complete and manually tested, you are allowed to point where unit tests might be helpful. This does not appy to other types of tests.

Read ~/.pi/AGENTS.md immediatelly and apply rules, if you don't, I'll notice it.
